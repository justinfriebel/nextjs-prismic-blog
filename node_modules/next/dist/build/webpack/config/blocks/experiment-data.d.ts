/// <reference types="lodash" />
import { Configuration } from 'webpack';
import { ConfigurationContext } from '../utils';
export declare const experimentData: import("lodash").CurriedFunction3<boolean, ConfigurationContext, Configuration, Configuration | Promise<Configuration>>;
